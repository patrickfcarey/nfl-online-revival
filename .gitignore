# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Machine-local rig details (address, user) — never commit; template is tracked
.env.local

# Capture output: transcripts and pcaps are large and may hold personal data
captures/*
!captures/.gitkeep
*.pcap
*.pcapng
*.jsonl

# coverage.py
.coverage
htmlcov/

# Built roster payloads and PS2 saves (game data; rebuild with tools/build_year_roster.py)
rosters/
